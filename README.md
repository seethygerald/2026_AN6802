# myfinancialapp

For interview review in GitHub Codespaces:

- All required environment variables are already configured as Codespaces secrets.
- You do not need to set up any API keys or `.env` values manually.
- Just run:

```bash
python3 app.py
```
